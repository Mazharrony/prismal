---
# Copy this file to `<slug>.mdx` and fill it in. Files prefixed with `_` are never published.
# Set `draft: false` only once every claim below is verified and the client has approved it.
title: Short outcome-led title, not the client name
client: Client name, or "Confidential" if under NDA
year: 2026
capability: custom-software # websites | custom-software | ai-automation
summary: One sentence a buyer can scan. What changed for the client.
outcome: The single hardest number you can defend. Leave empty rather than estimate.
draft: true
---

## The problem

What was actually broken. Concrete and specific — the state of things before.

## What we built

The system, in plain language. Name the real constraints you worked within.

## The result

Only verifiable outcomes. If a number cannot be attributed and defended, do not
publish it — describe the change qualitatively instead. Never estimate and round.
