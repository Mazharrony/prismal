# Where contact-form submissions are delivered.
# Any endpoint accepting a JSON POST works — Zapier/Make webhook, Resend, or your own inbox relay.
# Without this the form returns 503 by design rather than silently dropping leads.
CONTACT_WEBHOOK_URL=
